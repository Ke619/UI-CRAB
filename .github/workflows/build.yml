name: Build AppImage

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc libgtk-3-dev pkg-config libfuse2 libgstreamer1.0-dev

      - name: Compile
        run: |
          gcc headcrab_updater.c -o AppDir/usr/bin/headcrab_updater \
            $(pkg-config --cflags --libs gtk+-3.0 gstreamer-1.0)
          chmod +x AppDir/usr/bin/headcrab_updater
          chmod +x AppDir/AppRun
          cp BGM.wav AppDir/usr/bin/BGM.wav
          cp Click.mp3 AppDir/usr/bin/Click.mp3
          cp Hover.mp3 AppDir/usr/bin/Hover.mp3

      - name: Download appimagetool
        run: |
          curl -Lo appimagetool "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage"
          chmod +x appimagetool

      - name: Build AppImage
        run: |
          ARCH=x86_64 ./appimagetool AppDir HeadcrabUpdater-x86_64.AppImage

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HeadcrabUpdater
          path: HeadcrabUpdater-x86_64.AppImage
